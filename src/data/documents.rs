// Documents now persist in `public.documents` (Supabase) — see
// `backend/src/handlers/documents.rs`.  This file is left in place
// because `data/mod.rs` references it, but no in-memory seed survives.
